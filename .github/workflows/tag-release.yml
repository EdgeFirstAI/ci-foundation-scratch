# Proof caller — pin uses: to the same EdgeFirstAI/.github commit as ci.yml.

name: Tag Release

on:
  pull_request:
    types: [closed]
    branches: [main]

jobs:
  tag:
    if: github.event.pull_request.merged == true && startsWith(github.event.pull_request.head.ref, 'release/')
    uses: EdgeFirstAI/.github/.github/workflows/tag-release.yml@a2c8700493c160453bc789ffcf18cbe784e8aff1
    secrets: inherit
